@import '../../colors.styl';

.dropdown-menu-buttton
  position: relative
  padding: 0 20px

  &.active
    .dropdown-menu
      max-height: 500px
      transition: max-height 0.25s ease-in

  .dropdown-menu
    position: absolute
    background-color: #fff
    color: #000
    box-shadow: 0 0 0.5rem rgba(0, 0, 0, 0.3)
    overflow: hidden
    max-height: 0
    right: 20px
    transition: max-height 0.15s ease-out
    padding: 0 5px
  
  .dropdown-button .button
    box-shadow: none
    background-color: primary900
    padding: 0 10px